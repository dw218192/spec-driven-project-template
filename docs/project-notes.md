# Project notes

Project-specific agent instructions go here — domain rules, conventions, gotchas, the
things true of *this* project that the (template-owned) process doesn't cover.
`AGENTS.md` points the agent here, and `CLAUDE.md` imports it.

This file is **yours**: the template sync (`.agent/scripts/update_from_template.py`)
never touches it — which is exactly why project-specific instructions live here instead
of in `AGENTS.md`, so those stay syncable. Replace this placeholder with your own.
