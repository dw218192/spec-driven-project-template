# Spikes

Concluded spike evidence artifacts go here, numbered `docs/spike/SPIKE-<n>.md`.
Author with the **`spike`** skill (template:
`.claude/skills/spike/references/spike-template.md`); only `status: concluded` spikes
are citable.
